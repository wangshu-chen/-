<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bishe.teamproject.dao.UserinfoMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.bishe.teamproject.model.UserInfo">
                    <id column="id" property="id"/>
                    <result column="username" property="username"/>
                    <result column="password" property="password"/>
                    <result column="realname" property="realname"/>
                    <result column="sex" property="sex"/>
                    <result column="tel" property="tel"/>
                    <result column="email" property="email"/>
                    <result column="status" property="status"/>
                    <result column="type" property="type"/>
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
            id, username, password, realname, sex, tel, email, status, type
        </sql>
    <update id="updatePassordByIdAndNewPassword">
        update team_project.userinfo set password=#{pwd} where id=#{id}
    </update>
    <select id="queryUserInfoByUserNameAndPasswordAndType"
            resultType="com.bishe.teamproject.model.UserInfo"
            parameterType="com.bishe.teamproject.model.UserInfo">
        select id,type,username
        from team_project.userinfo
        where type=#{type} and password=#{password} and username=#{username};
    </select>
    <select id="queryUserInfoList"
            resultType="com.bishe.teamproject.model.UserInfo"
            parameterType="com.bishe.teamproject.model.UserInfo">
        select *
        from team_project.userinfo
        <where>
            <if test="tel!=null and tel!=''">
                and tel like '%${tel}%'
            </if>
            <if test="username!=null and username!=''">
                and username like '%${username}%'
            </if>
            <if test="id!=null">
                and id = #{id}
            </if>
        </where>
    </select>

</mapper>